# AGENTS.md

## Cursor Cloud specific instructions

This is a blank repository initialized with only a `README.md`. There are currently:

- No application code, services, or frameworks
- No dependency files (`package.json`, `requirements.txt`, etc.)
- No build system, linter config, or test framework
- No Docker or devcontainer configuration

When code is added, update this file with relevant development instructions (how to install deps, run services, lint, test, and build).
